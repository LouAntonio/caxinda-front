# URL base da API. Em dev usa-se o proxy do Vite (curl /api -> localhost:3000).
# Em produção apontar para o domínio da API, ex: https://api.caxinda.ao
VITE_API_URL=/api

# Client ID da aplicação Google (deve corresponder ao GOOGLE_CLIENT_ID do backend)
VITE_GOOGLE_CLIENT_ID=your_google_client_id